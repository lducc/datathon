# Notebooks

Canonical notebooks are grouped by task:

- `part1_mcq/`
- `part2_eda/`
- `part3_modeling/`

Part 2 notebooks build figures directly from the official dataset.
Part 3 notebooks show the recreated baseline and the shape-medium benchmark line.
The canonical final submission line is the currently promoted candidate in `models/final_meta_regime_ensemble.py`, generated by the modeling scripts under `models/` and `scripts/run_part3_pipeline.py`.
The final interpretability notebook for the Kaggle line is `part3_modeling/03_final_meta_fine_horizon_interpretability.ipynb`.
