# Paper

`paper/neurips/` contains the final write-up source and compiled PDF.

Canonical artifacts:

- source: `paper/neurips/main.tex`
- compiled PDF: `paper/neurips/main.pdf`

The paper should reference the final submission model `meta_fine_horizon_nnls`, while keeping `internal_bottomup_baseline`, `final_shape_medium`, and `meta_static_nnls` as benchmark or ablation shoutouts where needed.
